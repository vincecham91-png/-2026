{
  "name": "star-photo-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Star Photo Share System",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
